package melearningoop.week62;

// BaseLogger class (parent class)
public class BaseLogger {
    public void log(String message) {
        // Default implementation (empty, to be overridden by subclasses)
    }
}
